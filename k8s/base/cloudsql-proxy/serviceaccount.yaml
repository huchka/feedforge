apiVersion: v1
kind: ServiceAccount
metadata:
  name: cloudsql-proxy
  namespace: feedforge
  labels:
    app.kubernetes.io/name: cloudsql-proxy
    app.kubernetes.io/component: database-proxy
    app.kubernetes.io/part-of: feedforge
  # Workload Identity annotation — set via overlay patch per environment
  # annotations:
  #   iam.gke.io/gcp-service-account: feedforge-cloudsql-proxy@PROJECT_ID.iam.gserviceaccount.com
automountServiceAccountToken: true
